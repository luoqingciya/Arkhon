# T4 · 构建链路与三形态基准确认

> 状态：CLI 打包 + 真机安装脚本已跑通；Phone / Tablet 形态基准确认已完成；**Tablet 发现全量默认路由限制（`isDefaultRoute: true` 不生效，需收缩为受限路由）**；折叠屏展开态因无真机设备暂时搁置。
> 产出：命令、真机验证记录。

---

## 1. CLI 打包（已验证）

```powershell
# 环境
$env:DEVECO_SDK_HOME = "D:\Apps\03_Dev_Environment\14_DevEco\DevEco Studio\sdk"
$node  = "D:\Apps\03_Dev_Environment\14_DevEco\DevEco Studio\tools\node\node.exe"
$hvigorw = "D:\Apps\03_Dev_Environment\14_DevEco\DevEco Studio\tools\hvigor\bin\hvigorw.js"

# 构建（entry 模块，debug 模式，签名 HAP）
Set-Location "d:\Project\Teyvat-Arkhon\Arkhon"
& $node $hvigorw --mode module -p module=entry@default -p product=default -p buildMode=debug assembleHap --no-daemon
```

产物：`entry\build\default\outputs\default\entry-default-signed.hap`

---

## 2. 真机安装 + 启动脚本（已验证）

```powershell
$hdc = "d:\Apps\03_Dev_Environment\14_DevEco\DevEco Studio\sdk\default\openharmony\toolchains\hdc.exe"

# 检查设备连接
& $hdc list targets

# 覆盖安装 HAP（需先解锁屏幕）
& $hdc install -r "d:\Project\Teyvat-Arkhon\Arkhon\entry\build\default\outputs\default\entry-default-signed.hap"

# 清空日志 + 唤醒屏幕 + 启动应用
& $hdc shell "hilog -r"
& $hdc shell "power-shell setmode 600"
& $hdc shell "power-shell wakeup"
Start-Sleep -Seconds 2
& $hdc shell "aa start -a EntryAbility -b cn.luoqingciya.arkhon"
```

---

## 3. 真机验证记录

| 日期 | 设备 | 形态 | 结果 |
| --- | --- | --- | --- |
| 2026-09-13 | Mate 80 Pro | Phone | ✅ CLI 打包 + 安装 + 启动正常，主进程内核 v1.10.0 启动，隧道出网 200 |
| 2026-09-13 | 平板 | Tablet | ✅ 安装成功（需重新生成签名 profile 包含平板 UDID），主进程内核 v1.10.0 启动；**⚠️ 全量默认路由 (`isDefaultRoute: true`) 不生效** |

**备注**：Phone / Tablet 形态已验证。折叠屏展开态基准确认因无真机设备暂时搁置（T4 非阻塞项，可延迟到 M1 汇总前执行）。

---

## 3.1 平板限制详情与适配（2026-09-13）

**现象**：平板安装启动正常、VPN 扩展建链成功（fd=30、内核 attach code=0、节点 delay 693ms、扩展进程 loopback 200 OK），但**主进程流量无法进隧道**（`隧道出网(IP) code=2300007 Failed to connect`、`隧道出网(域名) code=2300006 Failed to resolve`）。

**根因**：平板系统不允许三方应用设置全量默认路由。设备 `/proc/net/route` 只有 `wlan0`/`ancowlan0` 子网路由，**没有 `0.0.0.0/0`**；对比手机可正常写入。系统流量未导向 `multitun-vpn1` 虚拟网卡 → 主进程 TCP 探针在直连路径上失败。

**适配**：`VpnExtAbility.ets` 新增平板分支 `buildTabletConfig()`——路由收缩为受限网段（IPv4 `10.22.0.0/16` + IPv6 `fdfe:dcba:9876::/64`，`isDefaultRoute: false`），保持虚拟网卡/DNS/内核链路不变。设备类型检测 `isTabletDevice()` 暂为占位（默认 phone），待接系统参数判断。

**影响**：
- 平板形态 VPN 隧道成立，但仅能接管受限网段内的流量（部分流量仍走直连）；
- 全量接管需系统级能力（系统应用签名 / root），非三方应用可达成范围 → **PoC 结论登记为「平板支持受限路由」**。

---

## 4. 三形态基准确认（Phone / Tablet 已完成，折叠屏搁置）

目标：在 Phone / Tablet / 折叠屏展开态分别安装启动，确认页面可达（冒烟基准）。

**当前状态**：
- Phone 形态（Mate 80 Pro）：✅ 已验证
- Tablet 形态（平板）：✅ 已验证
- 折叠屏展开态：⏳ 暂时搁置（无真机设备，T4 非阻塞项）

**备注**：折叠屏基准确认可延迟到 M1 汇总前执行，不影响 Phase 0 立项评审。

---

## 5. 已知限制

- `hvigorw` 需通过 DevEco 内置 node 执行（`$node $hvigorw`），直接调用 `hvigorw.bat` 在部分环境不可用
- 设备屏幕锁定时 `aa start` 失败，需先 `power-shell wakeup` + 手动解锁
